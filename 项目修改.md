# 项目修改记录

## 当前完成内容

本阶段已完成课程期末项目的账号系统基础结构，在第一阶段（基础导航框架）的基础上，新增了本地用户会话持久化、手机号格式校验、登录方式记录，以及完整的 Profile 页面与登出功能。应用保持完全离线，不依赖 Firebase、后端接口或任何外部 API。

---

## 已实现功能

### 阶段一：基础结构（已完成）

#### 1. 登录页作为应用首屏

已新增 `LoginPage`，并将其设置为应用启动后的第一个页面。

登录页当前包含：

- 应用标题
- 手机号输入框
- 密码/验证码输入框
- `Login` 按钮
- 模拟社交登录按钮：
  - Google
  - Facebook
  - Apple

#### 2. 主页面底部导航

已将主页面改为 `BottomNavigationBar` 五标签结构，包含：

- `Discover`
- `Itinerary`
- `Map`
- `Feedback`
- `Profile`

#### 3. Discover 页面

`Discover` 页面复用原有首页 UI，展示旅游推荐内容。

#### 4. 其他页面占位

`Itinerary`、`Map`、`Feedback` 页面当前为占位页，作为后续功能扩展入口。

---

### 阶段二：账号系统（已完成）

#### 5. 手机号格式校验

登录页的 `Login` 按钮现在会校验手机号格式：

- 使用正则表达式 `^\+?[0-9]{7,15}$` 验证
- 支持可选的 `+` 国际区号前缀
- 要求 7–15 位数字
- 格式不合格时在输入框下方显示红色错误提示，不跳转
- 输入内容变化时自动清除错误状态

#### 6. 用户会话本地持久化

新增 `UserSession` 工具类（`lib/util/user_session.dart`），基于 `shared_preferences` 包：

- `save(phone, provider)` — 登录成功后保存手机号与登录方式
- `load()` — 读取本地存储的用户会话
- `clear()` — 退出登录时清除数据
- `isLoggedIn` — 异步判断当前是否已有登录记录

#### 7. 自动登录

应用启动时（`main.dart`）会异步读取本地会话：

- 若已有登录记录，直接跳转到 `MainScreen`，跳过登录页
- 若无记录，显示登录页

#### 8. 社交登录记录登录方式

三个社交登录按钮（Google / Facebook / Apple）点击后：

- 分别传入对应的 provider 名称字符串
- 调用 `UserSession.save('', provider)` 保存登录方式（phone 留空）
- 跳转主页面

#### 9. Profile 页面（完整实现）

替换原占位页，展示完整用户信息：

- 头像占位图标（圆形深色背景）
- 显示用户手机号（社交登录时显示 `[Provider] Account`）
- 登录方式标签（`Signed in via [Provider]`）
- Phone 和 Login method 两行信息展示
- **Logout 按钮**：调用 `UserSession.clear()`，清除本地会话后跳回登录页，并清空导航栈

---

## 用户信息存储说明

### 存储位置

使用 `shared_preferences` 本地持久化，存储两个键值：

| 键 | 说明 |
|----|------|
| `user_phone` | 手机号登录时的手机号，社交登录时为空字符串 |
| `user_provider` | 登录方式：`'Phone'` / `'Google'` / `'Facebook'` / `'Apple'` |

### 说明

- 信息保存在设备本地，应用关闭后保留（支持自动登录）
- 点击 Logout 后数据被清除，下次启动回到登录页
- 不发送到服务器，不进行真实账号认证

---

## 当前阶段的设计定位

本项目目前属于：

**离线 mock 演示版本（含持久化账号状态）**

特点如下：

- 展示完整登录 → 主页 → Profile → 登出流程
- 手机号格式校验符合基本输入规范
- 登录状态跨会话保持（自动登录）
- 不涉及联网和真实账号系统

---

## 本阶段修改的主要文件

### `pubspec.yaml`

新增依赖：

- `shared_preferences: ^2.2.3`

### `lib/util/user_session.dart`（新增）

用途：

- 封装本地用户会话的读写与清除逻辑

### `lib/main.dart`

用途：

- 启动时异步检查登录状态
- 根据结果决定首页为 `LoginPage` 或 `MainScreen`
- `MyApp` 新增 `startLoggedIn` 参数控制首页路由

### `lib/screens/login_page.dart`

用途：

- 新增手机号格式校验逻辑（RegExp）
- 登录成功后调用 `UserSession.save()` 保存会话
- 社交登录按钮传入对应 provider 名称

### `lib/screens/profile_page.dart`

用途：

- 替换占位页，展示真实用户信息
- 从 `UserSession.load()` 读取并显示手机号与登录方式
- 提供 Logout 按钮，清除会话并返回登录页

### `test/widget_test.dart`

用途：

- 更新测试以匹配 `MyApp` 新增的 `startLoggedIn` 必填参数（传入 `false` 模拟未登录状态）

---

## 当前未实现内容

以下内容尚未开发：

- 注册功能
- 真实手机号短信验证
- 真实 Google / Facebook / Apple OAuth 接入
- 密码加密存储
- 行程生成（Itinerary 页面业务逻辑）
- 地图能力（Map 页面）
- 问卷/反馈业务逻辑（Feedback 页面）
- 推荐系统（个性化推荐）

## 后续建议

按课程项目节奏，下一步建议优先实现以下其中之一：

1. `Feedback` 页面离线问卷表单（课程评估需要用户测试数据）
2. `Itinerary` 页面基础数据结构与列表展示
3. `Map` 页面静态交互式地图模拟
